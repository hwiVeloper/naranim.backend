<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="dev.hwiveloper.app.woomin.mapper.ElectionMapper">
	<select id="getElectionTypes" resultType="Map">
		SELECT
			sg_type_code,
			sg_type_name
		FROM
			woomin_election_type
		ORDER BY CAST(sg_type_code AS INT)
	</select>
	
	<select id="getElections" parameterType="Map" resultType="Map">
		SELECT
			sg_id
		FROM woomin_election
		WHERE sg_type_code = #{SG_TYPE_CODE}
		ORDER BY STR_TO_DATE(sg_id, '%Y%m%d') DESC
	</select>
	
	<select id="getSdNames" parameterType="Map" resultType="Map">
		SELECT
			DISTINCT sd_name
		FROM woomin_sungeogu
		WHERE 1=1
		AND sg_type_code = #{SG_TYPE_CODE}
		AND sg_id = #{SG_ID}
	</select>
	
	<select id="getSggNames" parameterType="Map" resultType="Map">
		SELECT
			DISTINCT sgg_name
		FROM woomin_sungeogu
		WHERE sg_id = #{SG_ID}
		AND sg_type_code = #{SG_TYPE_CODE}
		AND sd_name = #{SD_NAME}
	</select>
	
	<select id="getWiwNames" parameterType="Map" resultType="Map">
		SELECT
			DISTINCT wiw_name
		FROM woomin_sungeogu
		WHERE sg_id = #{SG_ID}
		AND sg_type_code = #{SG_TYPE_CODE}
		AND sd_name = #{SD_NAME}
	</select>
</mapper>