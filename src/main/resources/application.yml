spring:  datasource:    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: ${DB_USER}
    password: ${DB_PASS}  jpa:    hibernate:      ddl-auto: update    generate-ddl: true    show-sql: false    database: mysql    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect  data:    rest:      base-path: /apiserver:  port: 80# keyskeys:  national-assembly-info-service: ${NATIONAL_ASSEMBLY_INFO_SERVICE}  agenda-schedule-info-service: ${AGENDA_SCHEDULE_INFO_SERVICE}  common-code-service: ${COMMON_CODE_SERVICE}  poll-place-service: ${POLL-PLACE-SERVICE}